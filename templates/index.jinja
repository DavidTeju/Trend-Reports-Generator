<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        body {
            width: 80%;
            margin: auto;
            padding: 2em 0;
        }

        label {
            margin-right: 0.5em;
        }

        input {
            margin: 0.5em;
        }

        label + input {
            margin-left: 0;
        }

        fieldset {
            margin: .5em;
            padding: .5em;
            border: black solid 1px;
        }

        legend {
            font-size: calc(1rem + .3vw);
        }

        .score-map {
            display: flex;
            justify-content: space-between;
            margin: .5em auto;
            align-items: center;
        }

        footer {
            margin-top: 2em;
        }

        /* Add style for inputs with names that end with sub_question*/
        input[name$="sub_question"] {
            display: block;
            margin: 0.5em 0;
        }

        select {
            width: auto !important;
            display: inline-block !important;
        }

        .form-top, .form-bottom {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .form-bottom {
            margin-top: 1.5em;
        }

        .form-top {
            margin-bottom: 1.5em;
        }
    </style>
</head>
<body>
{% block body %}
    <h1> Reports Generator </h1>

    <p> This tool will generate reports based on the data you provide. </p>

    <div class="">
        <a role="button" class="btn btn-primary" href="{{ url_for('load_config') }}">
            Load Existing Config
        </a>
        <a role="button" class="btn btn-primary" href="{{ url_for('create_config') }}">
            Enter New Config </a>
    </div>

{% endblock body %}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
        crossorigin="anonymous"></script>
</body>
<footer> Office of Institutional Effectiveness, Dominican University</footer>
</html>
